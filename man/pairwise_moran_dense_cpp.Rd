% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{pairwise_moran_dense_cpp}
\alias{pairwise_moran_dense_cpp}
\title{Compute Pairwise Moran's I with Chunked Dense BLAS (Best for Large Radii)}
\usage{
pairwise_moran_dense_cpp(
  data,
  coords,
  radius,
  sigma = -1,
  chunk_size = 500L,
  verbose = TRUE
)
}
\arguments{
\item{data}{Gene expression matrix (genes x cells)}

\item{coords}{Cell coordinates (n x 2 matrix)}

\item{radius}{Radius for neighbor search}

\item{sigma}{Gaussian sigma (default: radius/3)}

\item{chunk_size}{Cells per chunk (default: 500, tune for memory)}

\item{verbose}{Print progress messages (default: TRUE)}
}
\value{
List with moran matrix, weight_sum, and n_edges
}
\description{
Uses chunked dense matrix operations with BLAS for efficient computation
when many neighbors per cell. Processes cells in batches to limit memory.
}
\details{
For each chunk of cells, computes dense weight matrix and uses optimized
BLAS matrix multiplication for spatial lag. Much faster than streaming
when density > 5\% (i.e., > n/20 neighbors per cell).
}
