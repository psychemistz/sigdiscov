% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{pairwise_moran_chunked_cpp}
\alias{pairwise_moran_chunked_cpp}
\title{Compute Pairwise Moran's I Using Chunked Dense Approach (No Neighbor Limit)}
\usage{
pairwise_moran_chunked_cpp(
  data,
  coords,
  radius,
  sigma = -1,
  chunk_size = 1000L,
  verbose = TRUE
)
}
\arguments{
\item{data}{Gene expression matrix (genes x cells)}

\item{coords}{Cell coordinates (n x 2 matrix)}

\item{radius}{Radius for neighbor search}

\item{sigma}{Gaussian sigma (default: radius/3)}

\item{chunk_size}{Number of cells per chunk (default: 1000)}

\item{verbose}{Print progress messages (default: TRUE)}
}
\value{
List with moran matrix, weight_sum, and n_edges
}
\description{
Similar to Python GPU implementation: processes senders in chunks,
computes dense distance matrix for each chunk, extracts sparse entries.
No neighbor limit - finds ALL neighbors within radius.
}
\details{
Two-pass approach for memory efficiency:
\itemize{
\item Pass 1: Compute row sums only (for normalization)
\item Pass 2: Extract normalized triplets and build sparse matrix
}
}
